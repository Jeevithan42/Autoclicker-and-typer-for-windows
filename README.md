README

This is an auto clicker for windows built using python and python libraries. 

The UI is built using tkinter (a python library)

Easy to use UI - simply run the code and follow the instructions on the UI

Significant features include:
- Regular autoclicker with interval control
- Path planner, where you can add a list of destinations on your computer screen for the mouse to click through in order with associated intervals